# Secure Notes Frontend

React + Vite frontend for the Secure Notes app.

## Setup

```bash
npm install
cp .env .env.local
```

Update `VITE_API_URL` in `.env.local` to point to your backend.

## Run

```bash
npm run dev
```

## Build

```bash
npm run build
```
